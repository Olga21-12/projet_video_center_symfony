<footer class="footer bg-dark text-white mt-auto py-3">
    <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center">
        <div class="mb-2 mb-md-0">
            <a href="{{ path('home') }}" class="footer-link me-3">Home</a>
            {% if app.user %}
                <a href="{{ path('video_new') }}" class="footer-link me-3">Ajouter vidéo</a>
                <a href="{{ path('logout') }}" class="footer-link">Déconnexion</a>
            {% else %}
                <a href="{{ path('login') }}" class="footer-link me-3">Connexion</a>
                <a href="{{ path('register') }}" class="footer-link">Inscription</a>
            {% endif %}
        </div>
        <div class="text-white small">
            &copy; {{ "now"|date("Y") }} Video Center. Tous droits réservés.
        </div>
    </div>
</footer>
